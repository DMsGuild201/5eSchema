{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"version": "legacy-support",
	"type": "object",
	"description": "This schema redirects to the official homebrew schema build that should be used instead.",
	"items": {
		"$ref": "https://raw.githubusercontent.com/TheGiddyLimit/homebrew/master/_schema/homebrew.json",
		"$comment": "Note that this does not currently support the reduced schema that lints quickly for adventures and books. This functionality will be implemented soon :tm:."
	}
}